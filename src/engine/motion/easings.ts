import { easePremium } from "@/design-system/tokens/motion";

/** The one easing curve this project uses for eased (non-spring) motion. */
export const easings = {
  premium: easePremium,
} as const;
