<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Audit-field/RowVersion setters are internal — only the persistence layer (via its
         SaveChanges interceptor) may populate them, never a handler. -->
    <InternalsVisibleTo Include="Coffeshop.Persistence" />
  </ItemGroup>

</Project>
