# 39 — Commerce Implementation Readiness

Phase 0 deliverable, mirroring [25_IMPLEMENTATION_READINESS.md](25_IMPLEMENTATION_READINESS.md)'s format exactly. For each of Sprints 5.1-5.6: Prerequisites, Deliverables, Exit criteria, Verification steps, Rollback strategy, Dependencies. Sprint 5.0 is this Architecture Freeze itself (docs 29-40) — its own exit criterion is [40_COMMERCE_RC0_APPROVAL.md](40_COMMERCE_RC0_APPROVAL.md)'s go/no-go declaration, not a row in this table.

## Rollback strategy, the general case

Unlike [25_IMPLEMENTATION_READINESS.md](25_IMPLEMENTATION_READINESS.md)'s frontend sprints, these are **not** all backward-compatible-by-construction in the same trivial sense — a backend sprint typically ships a real database migration alongside code, and reverting code without reverting (or at minimum leaving inert) the schema change is a real hazard specific to this domain. The general case here is therefore two-part: **(1) revert the sprint's application code commits** (safe, same as the frontend case, since [37_API_STABILITY_POLICY.md](37_API_STABILITY_POLICY.md) means no later sprint's contract silently depends on an earlier sprint's internals), **and (2) leave the sprint's migrations applied but inert** rather than rolling the schema back — an added-but-unused table/column is harmless; a reverse migration run against a database that's already accumulated real rows from the reverted feature is where real data loss risk lives. A genuine schema rollback is only ever a last resort, called out explicitly per sprint below where it applies.

## Sprint 5.1 — Authentication Platform — **shipped**

| | |
|---|---|
| Prerequisites | [40_COMMERCE_RC0_APPROVAL.md](40_COMMERCE_RC0_APPROVAL.md) go decision; Docker Compose dev stack ([35_INFRASTRUCTURE_AND_DEPLOYMENT.md](35_INFRASTRUCTURE_AND_DEPLOYMENT.md)) running locally |
| Deliverables | `User`/`RefreshToken`/`EmailVerificationToken`/`PasswordResetToken`/`RoleDefinition`/`Permission` aggregates + EF Core migrations; all 10 auth endpoints (register/login/logout/refresh/me/verify-email/forgot-password/reset-password/sessions/revoke-session) per [33_AUTH_ARCHITECTURE.md](33_AUTH_ARCHITECTURE.md)'s sequence diagrams; seeded `Customer`/`Staff`/`Admin` roles + 11 permissions; rate limiting + audit-ready logging wired ([36_SECURITY_MODEL.md](36_SECURITY_MODEL.md)); new frontend `lib/auth-client.ts` + `stores/auth-store.ts` + 5 new routes + `AccountMenu`; a new `docs/41_BACKEND_DEVELOPMENT_STANDARDS.md` |
| Exit criteria | Every sequence diagram in [33_AUTH_ARCHITECTURE.md](33_AUTH_ARCHITECTURE.md) verified against the real running system, including the reuse-detection revoke-all path (deliberately triggered via curl and via an automated regression test, not just theorized); zero existing frontend store gained a required (non-optional) new dependency — **all met** |
| Verification steps | 42 automated backend tests (30 Domain, 6 Application, 6 Integration against a real Testcontainers Postgres) + 5 new Playwright e2e tests against the real API, all passing; a full manual curl-driven walkthrough of every endpoint against the real dev stack (Postgres/Redis/Mailhog/Seq); frontend `tsc`/`eslint`/`next build`/`vitest` (280 tests) clean; existing e2e regression suite spot-checked (`stabilization.spec.ts`) — **all run, all passing** |
| Rollback | General case applies. The `User` table itself had no prior data to protect (this was its first sprint), so even a full schema rollback would have been low-risk — moot now that it's shipped |
| Dependencies | None outside this sprint (first commerce sprint after the freeze) |
| **Real findings, not theoretical** | Six backend bugs found and fixed via live/automated verification (EF Core `.Value`-on-converted-property query translation, EF owned-entity INSERT-vs-UPDATE key generation, over-broad reuse-detection cascading to unrelated sessions, `UnitOfWorkBehavior` never persisting a mutation that preceded a thrown exception, FluentValidation silently skipping `internal` validator classes, an eagerly-read JWT signing key that only broke under layered test configuration) plus one frontend e2e regression (an expected anonymous-session-check 401 tripping a strict zero-console-errors test). Full detail: [docs/reviews/sprint-5.1-review.md](reviews/sprint-5.1-review.md). |

## Sprint 5.2 — Product Platform — **shipped**

| | |
|---|---|
| Prerequisites | Sprint 5.1 merged (JWT/permission middleware needed to protect the new admin-only product-mutation endpoints) — **met** |
| Deliverables | `Product`/`Ingredient`/`Category` aggregates + migrations (`InventoryItem` did **not** ship this sprint — Inventory is its own bounded context, correctly deferred rather than bundled in just because it was mentioned in the original sketch); seed migration from `features/menu/data/drinks.ts`/`features/composer/data/ingredients.ts` (`CatalogSeeder`); full REST surface — catalog reads, admin CRUD, publish/archive/restore lifecycle, image-by-URL management; PostgreSQL full-text search + trigram autocomplete ([34_PAYMENTS_NOTIFICATIONS_SEARCH.md](34_PAYMENTS_NOTIFICATIONS_SEARCH.md)) — no Redis read-through cache (also not built; the original sketch's caching layer has no real justification yet at this catalog's size, and adding one unused would be exactly the "config system for features that don't exist yet" [17_ZERO_REWRITE_POLICY.md](17_ZERO_REWRITE_POLICY.md) rules out); a new `/admin` area (products/categories/ingredients) — this project's first protected, permission-gated frontend route |
| Exit criteria | `ProductDto`/`IngredientDto` re-verified against real (not sketched) DTOs — one real shape correction found in the process: `ProductDto.Id`/`ProductSummaryDto.Id` is the real database `Guid`, not `Drink.id` directly (see [31_COMMERCE_ENGINEERING_CONTRACTS.md](31_COMMERCE_ENGINEERING_CONTRACTS.md)'s DTO table for the resolution — a client-derived slug, not a wire-format change). Seeded catalog renders correctly in the existing, unmodified `features/menu/`/`features/composer/`/`features/concierge/` UI with zero component *behavior* changes (component *internals* were rewired to fetch live data, per Phase 6's explicit brief — "zero regressions" was the real bar, verified by the full existing e2e suite passing unmodified, not "zero lines changed"). The C-07 discontinued-product/order-history test from [38_COMMERCE_RISK_REGISTER.md](38_COMMERCE_RISK_REGISTER.md) is **not yet applicable** — `Order` doesn't exist until Sprint 5.3, so nothing can reference a product across that boundary yet; carried forward, not silently dropped — **all applicable criteria met** |
| Verification steps | No feature flag — Phase 6 wired the real endpoints directly (matching Sprint 5.1's own precedent: nothing in production yet depends on the old static data, so a flag would have added real complexity to guard against a risk that doesn't exist pre-launch). 97 automated backend tests (68 Domain, 20 Application, 14 Integration against a real Testcontainers Postgres — includes new Domain tests for `PrepareForDeletion`/`ProductDeletedEvent` found during Phase 10's review), 299 frontend unit tests (19 new: store tests for `MenuStore`/`CategoryStore`/`SearchStore`, mapping-function tests for `slugify`/`productSummaryToDrink`/`ingredientDtoToIngredient`), 83 e2e tests across `menu`/`customizer`/`concierge`/`composer`/`cart`/`admin` on Chromium and Firefox (100% passing) and WebKit (100% passing outside a pre-existing, reproduced-on-Sprint-5.1's-own-`auth.spec.ts` rate-limiter interaction, not a Sprint 5.2 regression); full manual curl + live-browser (Playwright) walkthrough of the entire admin CRUD lifecycle and every new endpoint against the real dev stack; search relevance spot-checked against the real seeded catalog via live `psql` `ts_rank` queries, not a synthetic one; `tsc`/`eslint`/`dotnet build` clean throughout — **all run, all passing** |
| Rollback | General case applies. No prior `Product`/`Category`/`Ingredient` data exists to protect (this was their first sprint), so even a full schema rollback would be low-risk — moot now that it's shipped |
| Dependencies | Sprint 5.1 (auth middleware for admin mutation endpoints) |
| **Real findings, not theoretical** | Twelve real issues found and fixed via live/automated verification, spanning both the backend built earlier this sprint and gaps only surfaced by building the real admin UI consumer: an EF Core value-converter query-translation bug caught before it ever ran (`p.Sku.Value` in a LINQ predicate — the same class of bug Sprint 5.1 found for `Email`, this time prevented rather than discovered); an `OrderBy`-before-`Distinct` ambiguity in the autocomplete query; a Season-casing inconsistency; an unmapped `DbUpdateConcurrencyException` silently falling through to 500; two real DTO-drift gaps (`ProductSummaryDto` missing `Description`, `IngredientDto` missing `SortOrder` despite `UpdateIngredientCommand` requiring it on every call); a genuinely missing endpoint (`GET /api/v1/ingredient-categories` — `CreateIngredientCommand.IngredientCategoryId` had no way to be discovered); a missing `ProductDeletedEvent` (the only product lifecycle transition that raised nothing); a real validation gap (`CreateIngredientCommand` never checked `IngredientCategoryId` actually existed, and no database foreign key catches it either); and a `QuerySplittingBehavior` warning firing on every single product query, fixed by making the implicit default an explicit, documented choice rather than an unaddressed warning. Full detail: [docs/reviews/sprint-5.2-review.md](reviews/sprint-5.2-review.md). |

## Sprint 5.3 — Ordering Platform — **shipped**

| | |
|---|---|
| Prerequisites | Sprints 5.1-5.2 merged (orders reference both users and products) — **met** |
| Deliverables | `Order` aggregate + migrations, real end to end (`Cart`/`Coupon`/`Payment` did **not** ship this sprint — this sprint's own real brief scoped it to Ordering alone, not the original sketch's combined Cart+Order+Coupon+Payment+Inventory+SignalR bundle; each is its own bounded context, correctly deferred rather than bundled in just because the original sketch mentioned it, the same discipline `InventoryItem` got in Sprint 5.2's own record above); full CQRS command/query set; REST surface under `/api/v1/{orders,admin/orders}`; a real Postgres `order_number_seq` sequence; `stores/cart-store.ts`'s `placeOrder()` wired to the real endpoint — its *public shape* did change (async now, an added `idempotencyKey` parameter, `lastOrder` now a real `OrderDto` instead of a locally-fabricated `CompletedOrder`), a real, reasoned deviation from this row's original "unchanged" exit criterion below, not an oversight: the operation genuinely became a network call needing data (a real idempotency key) that a synchronous local function had no reason to need. New frontend: `lib/order-client.ts`, `/orders` + `/orders/[id]` (My Orders, Order Details, Order Timeline), `/admin/orders` + `/admin/orders/[id]` (the first admin area a `Staff` account, not just `Admin`, can reach) |
| Exit criteria | Every numbered scenario in [29_COMMERCE_ARCHITECTURE_FREEZE.md](29_COMMERCE_ARCHITECTURE_FREEZE.md) that touches Ordering *and is actually applicable to this sprint's real, narrower scope* re-verified against the real running system — scenarios assuming Payment/Inventory/Coupon (C-01/C-02/C-04/C-06 from [38_COMMERCE_RISK_REGISTER.md](38_COMMERCE_RISK_REGISTER.md), the chaos/load tests, the payment-provider-test-mode walkthrough) are **not yet applicable**, carried forward rather than silently dropped, for the same reason those aggregates didn't ship — nothing exists yet to test. `cart-store.ts`'s public shape claim above is corrected, not silently left standing (see Deliverables). The real, applicable bar instead: server-side price recalculation never trusts the client (verified live and via automated test), ownership/permission authorization never leaks order existence to a non-owner (verified live and via automated test), and no double-submission creates duplicate orders (a real bug found live during this sprint's own adversarial review, fixed, and locked in by both an automated integration test and a live concurrent-request repro) — **all applicable criteria met** |
| Verification steps | 172 automated backend tests (106 Domain, 40 Application, 26 Integration against a real Testcontainers Postgres), 302 frontend unit tests, 10 new order-specific e2e tests (`orders.spec.ts`, `admin-orders.spec.ts`) plus the full pre-existing suite (`cart.spec.ts`, `admin.spec.ts`, `customizer.spec.ts`) re-run with zero regressions on Chromium and spot-checked clean on Firefox (WebKit hit the same pre-existing `AuthPolicy` rate-limiter interaction Sprint 5.2's own record already names under sustained parallel test load — not a new regression, and independently confirmed via isolated re-runs); full manual curl + live-browser walkthrough of the entire order lifecycle (create → submit → pay → complete, and separately → cancel, and separately → fail) plus a real concurrent-request race test (two simultaneous `pay` calls on one order — exactly one succeeded, the other got a real 409) against the real dev stack; `tsc`/`eslint`/`dotnet build`/`next build` clean throughout — **all run, all passing** |
| Rollback | General case applies — this sprint shipped no payment/inventory traffic (see Deliverables), so the Sprint 5.3 sketch's own "exception to the general case" below does not apply to what was actually built. No prior `Order` data exists to protect (this was its first sprint) |
| Dependencies | Sprints 5.1, 5.2 |
| **Real findings, not theoretical** | Six backend bugs found and fixed via live/automated verification: an EF Core owned-type reference-sharing bug (`OrderTotals.Subtotal`/`.Total` sharing one `Money` CLR instance confused EF's `NavigationFixer`), a stale `CreatedAtUtc` in the create-order response (mapped before `SaveChangesAsync`'s interceptor had stamped it), a timeline-ordering bug (two events sharing an identical timestamp sorted non-deterministically — fixed with an explicit `Sequence` ordinal), a broken admin order search (`EF.Property<string>` still doesn't correctly translate `ILIKE` against a `HasConversion`-mapped column — searches by exact `OrderNumber` match plus `ILIKE` on the real plain-string guest fields instead), a live-verified double-submission bug (two concurrent identical checkout requests created two separate orders — closed with a client-generated `IdempotencyKey`, checked before any sequence value is consumed), and a validation hole (`GuestOrderInfo` re-derived a weaker ad hoc email check instead of reusing Identity's own already-proven `Email` value object). Full detail: [docs/reviews/sprint-5.3-review.md](reviews/sprint-5.3-review.md). |

## Sprint 5.4 — Administration Platform

| | |
|---|---|
| Prerequisites | Sprints 5.1-5.3 merged (the admin dashboard surfaces users, products, and orders, all of which must already exist) |
| Deliverables | **`/admin` itself is not new here** — Sprint 5.2 already shipped it (`/admin/products`, `/admin/categories`, `/admin/ingredients`, `useRequireManageProducts`'s permission guard), pulled forward from this sprint's original sketch once Sprint 5.2's own brief asked for it directly. This sprint *extends* that existing area — new sections (`/admin/orders`, `/admin/content`, `/admin/coupons`, `/admin/users`), never a rebuild of the guard/nav shell Sprint 5.2 already established, per [17_ZERO_REWRITE_POLICY.md](17_ZERO_REWRITE_POLICY.md). `Review`/`Favorite`/`ContentBlock`/`AuditLogEntry`/`SettingEntry`/`MediaAsset` aggregates + migrations; `IEmailProvider`/`IBlobStorageProvider` concrete implementations; admin CRUD endpoints for content/coupons; analytics read-side projections |
| Exit criteria | C-05 from [38_COMMERCE_RISK_REGISTER.md](38_COMMERCE_RISK_REGISTER.md) resolved (every mutable admin endpoint reviewed for `ETag`/`If-Match` support, not just the ones built first); every admin mutation produces a real, queryable `AuditLogEntry` ([36_SECURITY_MODEL.md](36_SECURITY_MODEL.md)'s security-events list re-verified against real log output); every new `/admin` section reachable only under the `Admin`/`Staff` permission policies, matching Sprint 5.2's own `useRequireManageProducts` precedent — an equivalent "logged in as Customer, sees Access Denied" test already exists and passes for `/admin/products` (`e2e/admin.spec.ts`); this sprint's own new sections get the same test, not a new gating mechanism |
| Verification steps | A full admin walkthrough (create product → publish CMS content → view analytics → process a refund) in a real browser; permission-boundary tests for each role |
| Rollback | General case applies to this sprint's *own* new sections (`/admin/orders`/`/content`/`/coupons`/`/users`, each independently revertable). `/admin/products`/`/admin/categories`/`/admin/ingredients` and the permission-guard shell are Sprint 5.2's, already shipped and out of this sprint's rollback scope entirely |
| Dependencies | Sprints 5.1, 5.2, 5.3 |

## Sprint 5.5 — Infrastructure Platform

| | |
|---|---|
| Prerequisites | Sprints 5.1-5.4 merged (there's a real application to deploy) |
| Deliverables | Production Docker Compose profile; OpenTelemetry/Serilog wired end to end; health checks; CI pipeline (build/test/image build/push) per [35_INFRASTRUCTURE_AND_DEPLOYMENT.md](35_INFRASTRUCTURE_AND_DEPLOYMENT.md); secrets management wired to the target cloud provider; SignalR Redis backplane verified under a real multi-replica run for the first time |
| Exit criteria | A rolling deploy (replica count ≥2) demonstrably serves traffic through the deploy with zero dropped requests (readiness-probe-gated); the intentional migration-failure drill (C-13, [38_COMMERCE_RISK_REGISTER.md](38_COMMERCE_RISK_REGISTER.md)) halts the deploy as designed; a trace spanning frontend request → API → payment provider call is visible end to end in the tracing backend |
| Verification steps | The rolling-deploy test and migration-failure drill above, run against a real staging environment, not just locally |
| Rollback | Infrastructure-specific: a bad CI/CD or Compose change rolls back via the previous known-good image tag, not a code revert — this sprint's own artifact *is* the deployment mechanism, so "rollback" means redeploying the prior version, which the pipeline itself must support (verified as part of this sprint's own exit criteria, not assumed) |
| Dependencies | Sprints 5.1-5.4 |

## Sprint 5.6 — Production Readiness

| | |
|---|---|
| Prerequisites | Sprint 5.5 merged (needs a real deployed environment to harden) |
| Deliverables | Load testing against realistic traffic shapes; security review pass against [36_SECURITY_MODEL.md](36_SECURITY_MODEL.md)'s full threat table; documentation finalization; final Creative Director Review extended to cover the new `/admin` surface and checkout flow (per this project's existing per-milestone gate, [09_CREATIVE_DIRECTOR_REVIEW.md](09_CREATIVE_DIRECTOR_REVIEW.md)) |
| Exit criteria | Every row in [38_COMMERCE_RISK_REGISTER.md](38_COMMERCE_RISK_REGISTER.md) marked "Open" has either moved to "Mitigated"/"Accepted" with real evidence, or is explicitly carried forward with a named owner and reason — no silent drop; CDR pass ≥9.5 on every in-scope category for the new `/admin` and checkout surfaces, matching the bar every prior milestone has held |
| Verification steps | The load test, the security review checklist, and the CDR process itself |
| Rollback | General case applies; by this sprint, any rollback of a specific finding's fix is scoped to that finding, not the whole sprint — matching the "Improvement Loop" precedent from [25_IMPLEMENTATION_READINESS.md](25_IMPLEMENTATION_READINESS.md)'s Sprint 2.6 |
| Dependencies | All prior Milestone 5 sprints |

## Related

[milestone-5-commerce-rfc.md](milestone-5-commerce-rfc.md) · [38_COMMERCE_RISK_REGISTER.md](38_COMMERCE_RISK_REGISTER.md) · [25_IMPLEMENTATION_READINESS.md](25_IMPLEMENTATION_READINESS.md) · [40_COMMERCE_RC0_APPROVAL.md](40_COMMERCE_RC0_APPROVAL.md)
